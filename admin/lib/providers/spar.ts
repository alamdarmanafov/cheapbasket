import { createWoltProvider } from './woltProvider';

export const SparProvider = createWoltProvider({
  market: 'spar',
  name: 'SPAR',
  woltUrl: 'https://wolt.com/az/aze/baku/venue/spar-azerbaijan',
});
