import { createWoltProvider } from './woltProvider';

export const BazarstoreProvider = createWoltProvider({
  market: 'bazarstore',
  name: 'Bazarstore',
  woltUrl: 'https://wolt.com/az/aze/baku/venue/bazarstore',
});
