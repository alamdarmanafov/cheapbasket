import { createWoltProvider } from './woltProvider';

export const TamStoreProvider = createWoltProvider({
  market: 'tam-store',
  name: 'Tam Store',
  woltUrl: 'https://wolt.com/az/aze/baku/venue/tam-store',
});
